# --- Core HTTP & async ---
requests
aiohttp                          # async media downloads

# --- AI providers ---
openai
google-genai
anthropic                        # Claude (claude-sonnet-4-6 default)
# Ollama uses plain HTTP — no SDK required, just have `ollama serve` running

# --- Configuration ---
python-dotenv

# --- TUI / rendering ---
rich
questionary                      # arrow-key navigable menus (graceful fallback)
pyvis                            # interactive HTML graphs

# --- HTML / web extraction ---
beautifulsoup4
trafilatura                      # main-content extraction (preferred)
readability-lxml                 # main-content extraction (fallback)

# --- Browser automation ---
selenium
#playwright                       # full-page screenshot engine

# --- Search / scraping helpers ---
google-api-python-client
phonenumbers                     # libphonenumber port for PII phone parsing
yt-dlp                           # video extraction across 1000+ sites

# --- Domain / network OSINT ---
dnspython                        # DNS resolver (A/AAAA/MX/TXT/SOA/NS/CAA…)
python-whois                     # WHOIS registry lookups

# --- Crypto / checksum primitives ---
pycryptodome                     # Keccak-256 for EIP-55 Ethereum address validation

# --- Document / file metadata ---
pypdf                            # modern PDF metadata (preferred)
PyPDF2                           # legacy PDF metadata (fallback)
python-docx                      # DOCX core properties
openpyxl                         # XLSX workbook properties + Excel export
exifread                         # image EXIF tags

# --- Web fingerprinting ---
python-Wappalyzer                # tech fingerprinting (preferred)
webtech                          # tech fingerprinting (fallback)

# --- PII detection (optional) ---
presidio-analyzer                # Microsoft Presidio entity detection

# --- Username enumeration ---
sherlock-project                 # 400+ social sites — pure-Python, works everywhere
# maigret                        # 3000+ sites — DO NOT enable on Termux / Python 3.13.
                                 # Pins aiohttp==3.8.x which has no aarch64-android wheels;
                                 # source build requires the full Rust toolchain (maturin).
                                 # Install on Linux/macOS only:  pip install maigret

# --- Email enumeration ---
holehe                           # ~120 services — checks where an email is registered
                                 # via password-reset flows. Complements HIBP (leaks).
